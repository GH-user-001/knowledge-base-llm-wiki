# Overview

This repository documents a workflow where an LLM incrementally builds and maintains a persistent wiki from raw sources.

The central insight is that the wiki is not just an output format. It is the main long-lived knowledge artifact.

Raw sources remain immutable.
The wiki is continuously updated.
The schema constrains and guides maintenance behavior.
